# ENGYE — The Bonded Broker · session boot

Full plan: `BONDED_BROKER_PLAN.md` (original brief; several decisions superseded — this file is the source of truth). Hard deadline: **Mon Jul 6, 2026, 11:59 PM ET**.
Pitch: an AI broker that routes paid x402 tasks to the best provider and posts a USDC bond on Arc behind every match, sized by its own calibrated confidence; failed validation auto-slashes the bond to the requester plus a treasury refund.

## Live on Arc testnet (gen-7: prague + gas-opt, default codegen — ALL FIVE CONTRACTS VERIFIED)
DEPLOY = `./scripts/deploy.sh` (verifies Solidity at deploy) then `bun scripts/verify-contracts.ts` (Vyper). **User rule: every deploy ships verified.**
| What | Address / id |
|---|---|
| BondedEscrow (VERIFIED, vyper) | `0x8565139c5702A8213Fc14F29E7DaeED4FD802a83` |
| RefundVault (VERIFIED, vyper, ~5 USDC float) | `0x4FB0FcB9832006604bd81c1a0059E78774774795` |
| ProviderStake (VERIFIED, vyper) | `0xf226A3B41bfb503c69F4cF99E19589795AF52265` |
| SessionAccount delegate — 6 role accounts (VERIFIED, vyper) | `0xB8e55588A02fd514b5fCD3107Aec3a5b73A97dB2` |
| IthacaAccount impl — ROOT delegate, session key live + passkey proven (VERIFIED, solidity) | `0x37014923e41C96671ebf5c700aF38B3e728077aa` |
| ROOT (user keystore, ~347 USDC, delegated to Ithaca) | `0xDAdaDA4E8038641212262Fd94E816d4A57CDC751` |
| ERC-8004 agentIds (role addresses never change — registrations survive redeploys) | ENGYE 845015 · P1 845016 · P2 845017 · P3(flaky) 845018 · VALIDATOR 845019 |
Root-model live proofs (root-smoke.ts): session intent `0xe5b2b8…2259`, passkey authorize `0x911adc…5e84`, passkey-signed intent `0xa1cfb7…9ec70`.
**Live app: https://engye.vercel.app** — pages `/ /dashboard /calibration /providers /status`; health at `/api/status`. Supabase project `engye` (`cwxstxusdetdllpkccnv`).

**Verification recipe (FINAL, schema walked via Phoenix websocket — see scripts/verify-contracts.ts):** Solidity: `forge script --verify --verifier blockscout --verifier-url https://testnet.arcscan.app/api/` (in deploy.sh). Vyper: v2 API only — `curl -F "files[0]=@std.json;type=application/json"` to `/api/v2/smart-contracts/<addr>/verification/via/vyper-standard-input`, compiler `v0.4.3+commit.bff19ea2`, settings EXACTLY `{optimize:true, outputSelection:{"*":["*"]}}`. GOTCHAS: (a) multipart field must literally be `files[0]` with `type=application/json` (curl or standard fetch+FormData both work — Bun/fetch FormData is NOT rejected; the real historical blocker was **Venom bytecode the verifier couldn't recognize**, fixed by default codegen); (b) NO evmVersion key — the verifier whitelist knows neither "prague" nor "default"; omit it, vyper 0.4.3's default IS prague (byte-identical, locally proven); (c) optimize must be BOOLEAN true; (d) flat outputSelection `{"*":["*"]}`; (e) **VENOM CANNOT BE VERIFIED** — the verifier drops `experimentalCodegen` → bytecode mismatch, so foundry.toml uses DEFAULT codegen (venom abandoned in gen-7); (f) failures are silent over REST — join `wss://testnet.arcscan.app/socket/v2/websocket?vsn=2.0.0` channel `addresses:<addr>` for `verification_result`; (g) transient 502s happen — resubmit.

**Account architecture (final):** ROOT delegates to **IthacaAccount** (on-chain code = `0xef0100` + impl, confirmed) — the agent's session key is authorized as a super-admin Secp256k1 key; the 6 role accounts stay on our SessionAccount delegate (manager=ROOT) — all six re-pointed **2026-07-05** from the legacy venom build `0x31dc3a57774fb5d664942adf2daf49ebf584061c` (unverifiable) to the verified default-codegen `0xB8e555…7dB2` via `scripts/repoint-roles.ts` (7702 storage survives; `manager`/`signers` intact — plan in `docs/superpowers/plans/2026-07-05-repoint-roles-to-verified-delegate.md`). Role on-chain actions sign with the role's OWN key (msg.sender==self), so the delegate runs only on session→`role.execute` and safe-mint callbacks. `lib/ithaca.ts` = relay-free ERC-7821 intents (session EOA signs + submits; encodings proven in `contracts/test/IthacaRoot.t.sol` incl. WebAuthn). Passkey/WebAuthn is PROVEN on-chain (root-smoke.ts authorizes a software P-256 passkey and it signs a live intent; Solady P256 verifier present on Arc at `0x000000000000D01eA45F9eFD5c54f037Fa57Ea1a`, RIP-7212 absent) — but there is **no browser enrollment UI yet** (deferred; only lib/ithaca.ts holds passkey code). **PROTOCOL CARVE-OUT: Circle Gateway buyers sign EIP-3009 via ecrecover — rail-A payments always use the role's own EOA key (in .env.local); a session key can NEVER sign a Gateway payment.** NOTE: `delegate-roles.ts`'s root-hop uses the OLD SessionAccount ABI; roles are already set up, so it's idempotent — if new roles are ever added, port the hop to `rootExecute()` from lib/ithaca.ts.
GOTCHAS locked in: (a) x402-batching **v3** defaults to the production facilitator — Arc testnet needs the explicit testnet URL (done in lib/x402.ts); (b) 7702 delegates MUST implement onERC721Received — ERC-8004 safe-mints (SessionAccount has it); (c) delegated EOAs work as Gateway buyers.

## Phase state — all six phases DONE (engineering); Phase 6 has outward-facing items left for the human
- [x] **Phase 0** — env & tooling: repo scaffolded (Next 16 + Bun), forge+vyper 0.4.3 wired, wallets generated, reference/ clones digested, migrations authored.
- [x] **Phase 1** — DONE (100%): e2e gasless x402 payment on Arc from the 7702-delegated demand account; payment row lands in Supabase.
- [x] **Phase 2** — DONE: contract system deployed (gas-opt, **default codegen** — venom dropped as unverifiable) + on-chain acceptance (bond→release, bond→slash, idempotent vault refund; Arcscan links in escrow-smoke output). **39/39 tests** (BondedEscrow 13, SessionAccount 10, ProviderStake 9, RefundVault 4, IthacaRoot 3). The core trio, one resolver, one shared bytes32 match key:
  - `BondedEscrow.vy` — bond + on-chain `decision_hash` (keccak of the broker's full decision JSON, committed before money moves) + permissionless `claim_timeout()` past the per-bond deadline.
  - `RefundVault.vy` — treasury float; price refunds **once-per-match enforced on-chain** (idempotent by construction); float visible for dashboard reconciliation.
  - `ProviderStake.vy` — provider co-insurance; on fail the resolver slashes `min(stake, bond)` to the requester ON TOP of the broker bond; unstake cooldown-gated (1h > match TTL) so exits stay slashable; broker LLM reads `stakes(provider)` as a routing signal.
  Redeploy is one command now: `./scripts/deploy.sh` fills the addresses; agents already registered (845015–845019).
- [x] **Phase 3** — DONE, acceptance PASSED on PRODUCTION: bonded PASS flow (bond released) + flaky FAIL flow (slash + stake-slash + vault refund; requester nets price+bond) with Arcscan links; full ERC-8004 writes per match. LESSONS: (a) validationRequest must be filed by the AGENT'S OWN wallet (in-house providers self-file; external providers file at onboarding); (b) qwen3 fallback needs `reasoning_effort` none|default (per-model in lib/llm.ts); (c) broker prompt must frame BONDING AS THE PRODUCT or the model hides in best_effort; (d) never make the LLM copy UUIDs — registry table uses P1/P2 aliases mapped server-side; (e) RISK_MARGIN=**0.5** + Bayesian pseudo-count seeding so fresh providers clear the EV gate at testnet prices. **ERC-8004 promoted into this lifecycle** (lib/erc8004.ts, signatures verified vs reference ABIs): per match — provider `validationRequest`, validator `validationResponse` (+ deliverable hash), broker `giveFeedback` (feedbackHash=matchKey). One bytes32 matchKey links escrow + validation + reputation. Per-match footprint ~5 tx on the pass path (bond, validationRequest, validationResponse, release, feedback); the fail path adds slash + stake-slash + vault refund. + batched Gateway settlement.
- [x] **Phase 4** — DONE: `/api/registry` paid-probe onboarding (SSRF-guarded: 402-shape check → real paid call → validator-scored Bayesian prior); 3 in-house providers (`/api/inhouse/{quote,summarize,flaky}`, flaky fabricates ~35% to force visible slashes); **(bond-aware worker A/B — TESTED & REVERTED 2026-07-06, don't redo):** hypothesis "the worker performs better if told a bond rides on it" is **NOT supported**. Wired a `BOND_AWARE_WORKER` flag that prepended bond stakes to the worker prompt + randomized bonded in-house matches into aware/baseline arms; forced the (signal-rich) flaky provider via `max_price 0.0009`. Result on flaky: −24.6pp at n≈15/arm **regressed to −6.4pp (p≈0.5) at n≈40/arm** — i.e. noise, no measurable effect; reliable providers were at the 100% ceiling. Confirms the review's thesis: **bonding is accountability/selection/calibration, not worker motivation** (the worker is bond-blind by design and loses nothing by it). All experiment code reverted to clean baseline; `BOND_AWARE_WORKER` removed from Vercel. autonomous buyer (LLM buy/skip judgment, atomic budget check-and-decrement via `spend_budget` RPC, `source=demand_agent` tagging); `.github/workflows/demand-agent.yml` (SHA-pinned actions, cron `*/5` + workflow_dispatch). **(2026-07-06 refactor):** the buyer is now a first-class **eve subagent**, `agents/demand.ts` RETIRED. Everything lives in `lib/demand.ts` — `runCycle` + a CLI runner `runCli(opts)` + read-only `demandStatus` (x402 imported DYNAMICALLY inside runCycle so the eve bundle stays lean & next/server-free). The GitHub cron + `demand:loop` invoke it as `bun run demand` → `bun -e "import('./lib/demand.ts').then(m => m.runCli())"` (GitHub Actions is still the only holder of DEMAND_PRIVATE_KEY). `agent/subagents/demand/` (agent.ts + instructions + `tools/demand_status`) lets the broker delegate demand-desk questions — read-only by design since the signing key isn't in the eve/Vercel runtime, so live buying stays on the cron. NOTE: GitHub's scheduled cron had not fired on its own as of last check — `workflow_dispatch` runs succeed; the reliable fallback for judging windows is the local loop `bun run demand:loop` (script added).
- [x] **Phase 5** — DONE: ENGYE Design System imported from claude.ai/design (project `5cd6d5da`; the plan's "DESIGN_HANDOVER.md" was never a file — the design lived in a Claude Design project). NOTE: subagents CANNOT call DesignSync (auth is session-scoped to the main agent) — fetch design files inline. Implemented: `app/globals.css` token system (marble `:root` / black-figure `.dark`), `components/ui/{primitives.tsx,Button.tsx,Input.tsx,CopyButton.tsx}` (9 core comps, lucide-react), landing `app/page.tsx` (pencil bar + hero + frieze + claim + live stat wall + register curl + footer), AppShell + `/dashboard` (realtime LiveFeed via supabase browser channel, stats, decisions rail from real broker reasoning, Flow panel), `/calibration` (SVG chart from settled buckets), `/providers` (leaderboard + working RegisterForm→/api/registry). Wired to live Supabase (public read = anon key, RLS public-select), deployed, visually verified both modes. `design-system/` (fetched reference) is gitignored. **⚠️ VISUAL DESIGN SUPERSEDED 2026-07-06** — the marble/clay/gold system was retired for the ENGYE ink/stone/verdigris handoff; see **Design system v2** below. The Phase-5 *plumbing* (Supabase wiring, realtime, SVG chart, RegisterForm→/api/registry) all survives; only the skin changed.
- [~] **Phase 6** — ENGINEERING DONE: per-IP rate limits (quote 20/min, registry 5/hr — 429 verified), treasury circuit breaker (<1 USDC pauses matching), 15s provider-call timeout, ATOMIC quote claim (open→executing conditional update prevents double-execution/double-pay; releases on settle fail), SSRF guard on the public registry, `/api/status` + `/status` reconciliation (live on-chain balances + verified contracts), 'Bonded by ENGYE' badge endpoint, judge-facing `README.md` + Apache `LICENSE`. Post-hardening Phase-3 acceptance re-ran PASS+FAIL green. **REMAINING = OUTWARD-FACING (need the human):** (a) make repo public (`gh repo edit dadadave80/engye --visibility public --accept-visibility-change-consequences`); (b) Discord onboarding posts (Canteen + Arc); (c) `arc-canteen update product` + `update traction`; (d) file the v1 submission; (e) optional ArcOSS repo split (`engye-bonded-escrow` = contracts + lib/x402 + lib/escrow); (f) recruit ≥1 external provider. Drafts for a–d were handed to the user.

## dApp redesign (2026-07-02 — user: "it's not a dApp, just a dashboard; make contract fns user-facing")
Spec: `docs/superpowers/specs/2026-07-02-engye-dapp-redesign-design.md`. Studied the winning Agora app (Mimir Markets — marketing landing → Launch App → Connect + user-signed create/stake). Turned ENGYE from read-only-dashboard-over-server-keys into a real dApp. DONE + deployed:
- **Wallet layer** (`components/wallet/`): wagmi (injected/EOA) + `PasskeyProvider`; `ConnectButton` (menu: "Browser wallet · full flow" / "Passkey · no extension") in the app shell top-right; `useWallet()` unifies EOA vs passkey. `WalletProvider` mounted in root layout.
- **Flow 1 — Post a Task (`/post`, hero):** connect (EOA) → `/api/broker/quote` → live bonded quote → **pay from the user's own wallet** via hand-rolled browser x402 (`lib/gatewayBrowser.ts`: deposit = approve+`GatewayWallet.deposit`; pay = `walletClient.signTypedData` of the EIP-3009 `TransferWithAuthorization` under the `GatewayWalletBatched` v1 domain → base64 `Payment-Signature` header). Encoding verified against the SDK internals (GatewayClient is private-key-only; BatchEvmScheme/hand-roll are the browser paths). requester_wallet = connected address, so bond+refund land in the user's wallet on fail.
- **Flow 2 — Provider stake (`/stake`, `StakePanel`):** approve + `ProviderStake.stake`, `request_unstake` (cooldown), `withdraw`; live reads.
- **Flow 3 — Rescue a stuck bond (`ClaimCard` on `/status`):** permissionless `BondedEscrow.claim_timeout` from any connected wallet.
- Client bindings: `lib/clientChain.ts` (addresses via NEXT_PUBLIC_*, ABIs, browser publicClient). NEXT_PUBLIC_{RPC_URL,ESCROW,PROVIDER_STAKE,USDC}_ADDRESS added to .env.local + Vercel.
- Landing CTAs → Post a task / Watch the market / Stake. Nav: Post a Task · Dashboard · Providers · Stake · Calibration + Connect.
  **(⚠️ chrome/nav/`ConnectButton` here SUPERSEDED by Design system v2 — the wallet FLOW below still holds; only its presentation moved to `WalletControl`.)**

## Design system v2 — ENGYE ink/stone/verdigris (2026-07-06, SOURCE OF TRUTH for all visuals)
Retired the marble/clay/gold system for the **`design_handoff_engye_site/`** handoff ("a trading desk excavated from the agora": cool stone × ink × verdigris; **cinnabar ONLY when money is destroyed**; **no gold** — `--gold*` are legacy aliases of `--accent*`). Full brief in that dir's `README.md` + `BRAND.md`. Live + verified on engye.vercel.app.
- **Theme mechanism CHANGED: `[data-theme]` on `<html>`, NOT the `.dark` class.** `app/globals.css` = `engye-theme.css` adopted near-verbatim (tokens both themes: bare `:root` = dark/ink default, `:root[data-theme="light"]` = Pentelic stone) + the full component layer (`.card .stat .seal .quote-card .table-wrap .btn .chip .page-head .kicker .ledger-rule .site-header .site-footer .chat .verdicts .decision .flow` chart …) — **class names are the API**. Fonts: Cinzel (wordmark/kicker/seals ONLY, never sentences) · Geist (body + headings) · **IBM Plex Mono** (every number/hash, `tabular-nums`) · EB Garamond italic (ἐγγύη + quotes). A **compat-alias layer** in `:root` maps the old vocab-1 tokens (`--muted-foreground --border --foreground --card --primary …`) onto the new ones, so any not-yet-reskinned inline style still renders on-palette — retire aliases as consumers migrate.
- **Theme script:** `engye-theme.js` inlined VERBATIM as a pre-paint `<script dangerouslySetInnerHTML>` in `app/layout.tsx` (`<html suppressHydrationWarning>`, single `theme-color` meta). First visit follows the OS (`prefers-color-scheme`), an explicit coin flip persists in `localStorage["engye-theme.v2"]`; `.theme-anim` gates transitions after first paint. The two-faced obol **`CoinToggle`** (`role="switch"`, moon/sun faces, 220ms `rotateY`) is static markup — the script wires clicks via event **delegation** on `.coin-toggle`, so it works inside server components.
- **Chrome:** `AppShell` = `SiteHeader` (8-link nav w/ `aria-current`) → container'd `<main>` → `SiteFooter`. `WalletControl` (`.wallet-btn`/`.wallet-menu`) = the real `useWallet`/`usePasskey`/`useDisconnect`/`ConnectModal` flow (replaced the design-kit mock + the old `ConnectButton`). `ObolMark` = inline `currentColor` owl-coin. **`ConnectGate`** = the disconnected `.gate` on `/post /stake /account`. **No skip-link** (it flashed on client nav; landmarks provide bypass). Disconnect/Sign-Out are NOT cinnabar.
- **Landing** (`app/page.tsx` + `app/landing.css`, choreography in `components/landing/LandingClient.tsx`): its own light header + colophon, **scoped under `.engye-landing`** because the landing's token vocabulary flips meaning (`--ink` = the dark GROUND there, opposite of the app's `--ink` = foreground text) — scoping prevents a global collision. Bound to live data (`getTotals`/`getFeed`/`getWalkthrough`): hero cycles real recent bonds, "follow one bond" traces one real settled bond's Arcscan hashes (x402 step labelled honestly — no fabricated tx), the slash scene + agora board use a real slashed match. Vanilla IO reveals / hero cycle / slash orchestration; reduced-motion aware; keyframes namespaced `lv-*`.
- **Social + favicon:** static `app/opengraph-image.png` + `app/twitter-image.png` (the handoff's final 1200×630 stone card, with `.alt.txt`) replaced the dynamic `opengraph-image.tsx`. `app/icon.svg` = the new **stone-backed** ink obol (the old ink+gold favicon is gone). `public/assets/obol-mark.svg` refreshed (still used by `ConnectModal`).
- **Removed as orphaned:** `AppHeader`, `LandingHeader`, `wallet/ConnectButton`, `ui/CountUp`, `ui/GravityStars`. Every one of the landing + 10 routes (`/hire /agora /post /dashboard /providers /stake /calibration /account /status /m/[matchKey]`) is on the handoff classes. Money never displays 0.00 (small real values seeded; landing floor strip drops the at-stake clause when nothing is bonded).
## Passkey = Circle Modular Wallets (2026-07-05 — replaced the Porto/Ithaca hand-roll)
id.porto.sh worked on mobile but OUR Porto+IthacaAccount+self-relay passkey didn't — so it was migrated to **Circle Modular Wallets** (passkey-owned MSCA on Arc, gasless via Circle Gas Station). `lib/circleWallet.ts` (client-only — the Circle SDK needs `window`, so it CANNOT run in a route/script): `toPasskeyTransport`+`toModularTransport` → register/login (`toWebAuthnCredential`) → `toCircleSmartAccount`; `sendCalls` = one gasless userOp (`paymaster:true`). `passkeyClient.ts` signUp/login derive the MSCA + register it server-side; `payForQuote` pays via a userOp then binds the tx to the quote (`/api/passkey/pay` post-hoc receipt validation). `useAccountActions.send`/stake/claim all route through the userOp. **CONSOLE DEPENDENCY:** `NEXT_PUBLIC_CLIENT_KEY` (TEST_, domain-bound to `engye.vercel.app` — server/localhost calls 401) + `NEXT_PUBLIC_CLIENT_URL` (in .env.local + Vercel) + a **Gas Station policy for Arc Testnet** (else userOps AA33). `/api/passkey/provision` registers the client account (a bogus row is harmless — payer allow-list only) + sponsors 0.25 USDC bounded by `DAILY_SPONSOR_CAP_USDC=10` (server-side address derivation is infeasible — SDK needs window). VERIFIED: sign-in biometric (user-confirmed on device), `rp_getRegistrationOptions` 200 for engye.vercel.app w/ email username, provision 200+sponsor. NOT yet exercised: a live gasless userOp PAYMENT (needs a real device biometric — can't run headless; no virtual-authenticator tool).

**USEROP GAS GOTCHA (2026-07-06):** the client-side gasless userOp **fees MUST come from Circle's own oracle** — the bundler in `lib/circleWallet.ts clients()` sets `userOperation.estimateFeesPerGas` to `circle_getUserOperationGasPrice` (via `getUserOperationGasPrice`, `.medium`). viem's DEFAULT fee estimation does NOT work against Circle's Arc bundler → userOps ship with missing/invalid gas fields and the bundler rejects with **"Missing or invalid parameters"** (this is why the first live client payment failed). Also: pay with Circle's canonical `encodeTransfer` (`{to,data}`, NO `value`) via `sendUsdcTransfer` — a hand-rolled `{to,value:0n,data}` was the other suspect. Both applied. (Untested on-device — needs a biometric.)

**PASSKEY-ONLY (2026-07-06):** external/injected (EOA) wallets are DISABLED this version via `EXTERNAL_WALLETS_ENABLED = false` in `components/wallet/useWallet.ts` — an EOA connection is no longer treated as a usable app account, and `ConnectModal` hides its Browser Wallet option. One flag flip re-enables the whole EOA path (the modal reads the same flag). The recovery flow still connects an EOA independently (unaffected). The Porto-style `ConnectButton` was removed; the header uses `WalletControl`. Both wallet modals portal to `document.body` (the sticky `.site-header`'s `backdrop-filter` was making it the containing block for their `position:fixed` overlay → pinned to the header).

**PASSKEY RECOVERY (2026-07-06, Circle `recoveryActions`):** register an **EOA WALLET OF CHOICE** as a recovery key (user direction — NOT a generated mnemonic), replacing the old Ithaca-based recovery (`getKeys`/`authorize` don't exist on a Circle MSCA). `lib/circleWallet.ts`: `registerRecovery(credential, recoveryAddress)` (`bundler.extend(recoveryActions).registerRecoveryAddress`, gasless) with `ensureDeployed` (MSCAs deploy lazily on the first OUTBOUND userOp — a received sponsor doesn't deploy them — so it does a gasless no-op self-call first) + `friendlyUserOpError` (AA33/balance); `recoverWithWallet(walletClient, username)` (mint a fresh passkey → the recovery wallet signs `executeRecovery` via `walletClientToLocalAccount` → swaps the MSCA owner). UI: `RecoveryModal` (account page — EIP-6963 wallet picker → read the chosen wallet's address → the passkey signs the on-chain registration; `recoveryStore` holds the registered address for the label) + `ConnectModal` "Recover a lost passkey" view (connect the recovery wallet → it signs the swap). The recovery wallet-connect is independent of the passkey-only app gate. **NOT yet exercised:** both flows need a real wallet + device biometric (headless can't) — verify the register + restore round-trip on-device; uses the same Gas Station policy (active). Confirm on-device: after `executeRecovery` the recovered address resolves to the ORIGINAL account.

**OTHER HONEST GAPS:** (a) Provider register still POSTs server-side (no wallet-proven ownership sig yet). (b) The EOA pay/stake/claim flows are BUILT but DISABLED this version (passkey-only); re-enable via the flag above to exercise them.

## Account rollout (COMPLETED — historical reference)
Done: (1) human identified funded keystore `daveKey` → ROOT `0xDAda…C751`; (2) funded the session EOA; (3) deployed SessionAccount; (4) atomic `add_signer --auth` delegating root + registering session key; (5) `bun scripts/delegate-roles.ts` delegated all 6 role accounts (manager=ROOT), self-registered the session key, funded broker/demand/others. Then root was **re-delegated from SessionAccount to IthacaAccount** (the final root delegate); SessionAccount now serves only the 6 role accounts. To re-run any of this after a redeploy: `bun scripts/delegate-roles.ts` (idempotent).

## Human/infra status — ALL SET UP
1. ~~Faucet~~ REPLACED & DONE: EIP-7702 keystore-root model; no faucet used.
2. ~~API keys~~ DONE: `GROQ_API_KEY` set. **No Anthropic** — `lib/llm.ts` is Groq-only (broker `openai/gpt-oss-120b`, validator+demand `openai/gpt-oss-20b`, failover `qwen/qwen3-32b`). Injection canary held.
3. ~~Supabase~~ DONE: project `engye` (`cwxstxusdetdllpkccnv`), keys in .env.local, schema applied (migrations 0001+0002). Management-API token: `security find-generic-password -s "Supabase CLI" -w` → strip `go-keyring-base64:` → b64decode → `sbp_…`; POST `/v1/projects/<ref>/database/query` needs a custom User-Agent (default python UA gets Cloudflare 403).
4. ~~Vercel~~ DONE: project `engye` (scope david-dadas-projects), env vars in production (SESSION/DEMAND private keys deliberately excluded). Redeploy: `vercel deploy --prod --yes --scope david-dadas-projects`.
5. ~~GitHub~~ DONE: repo `dadadave80/engye` (**still PRIVATE — make public before submission**), Actions secrets: DEMAND_PRIVATE_KEY, GROQ_API_KEY, SUPABASE_SERVICE_ROLE_KEY, NEXT_PUBLIC_SUPABASE_URL, RPC, APP_URL (no ANTHROPIC — Groq-only).
6. ~~DESIGN_HANDOVER.md~~ RESOLVED: the design lived in a Claude Design project, implemented in Phase 5.

## Operating rules
1. Commit at each green checkpoint with a descriptive message. Every deploy ships verified.
2. Before writing chain/payment code, consult the Circle Skills + `~/.arc-canteen/context/` bundle; copy working patterns from `reference/`.
3. Testnet only. Never commit secrets (`.env.local` gitignored; README is the only tracked markdown). Never hardcode addresses the skills/docs can resolve.
4. Log Circle-tooling friction in `FEEDBACK.md` ($500 DX prize) — currently 7 items.
5. `JUDGES.md` is superseded by the judge-facing `README.md` (which shipped in Phase 6); keep README current, treat JUDGES.md as scratch.

## Ground truth (resolved Phase 0 — do not re-derive)
- Chain: Arc Testnet, chainId **5042002** (0x4cef52). RPC: personal via `arc-canteen rpc-url` (public `https://rpc.testnet.arc.network`). Explorer: https://testnet.arcscan.app
- viem: `import { arcTestnet } from 'viem/chains'` — never hand-roll the chain.
- **USDC = `0x3600000000000000000000000000000000000000`** — native gas asset with an optional ERC-20 interface on the SAME balance. ERC-20 view: 6 decimals. Native/gas view (`msg.value`, `parseEther`): 18 decimals. Never mix the two.
- Gateway Wallet (EIP-712 `verifyingContract`, domain `GatewayWalletBatched` v1): `0x0077777d7EBA4688BDeF3E311b846F25870A19B9`. Facilitator: `https://gateway-api-testnet.circle.com`. CCTP/Gateway domain: 26.
- SDK: **`@circle-fin/x402-batching` v3.2.0** — seller `BatchFacilitatorClient` from `/server` (prefer `settle()` directly; **construct with `{url: FACILITATOR_URL}` — v3 defaults to PRODUCTION**), buyer `GatewayClient` from `/client` (`.deposit()`, `.pay(url,{method,body})`, `.getBalances()`). Buyers must be EOAs (ecrecover; delegated EOAs OK, SCA/1271 not). Buyer auth `validBefore` ≥ 7 days out.
- ERC-8004 canonical registries LIVE on Arc: Identity `0x8004A818BFB912233c491871b3d84c89A494BD9e`, Reputation `0x8004B663056A597Dffe9eCcC1965A193B7388713`, Validation `0x8004Cb1BF31DAf7788923b405b754f57acEB4272`. ABIs in `reference/erc-8004-contracts/abis/`.
- **LLM layer** (`lib/llm.ts`): per-role Groq models above; attempt order primary → primary+validation-error → fallback → fallback+error. GOTCHA: gpt-oss are reasoning models — hidden reasoning consumes `max_completion_tokens`; keep ≥2048 headroom + `reasoning_effort:"low"` for gpt-oss, `"none"|"default"` for qwen3 (per-model in the wrapper). `response_format:{type:"json_object"}` works.

## Vyper toolchain rules (binding)
1. Pinned **Vyper 0.4.3** (`0.4.3+commit.bff19ea2`, /Users/dadadave/.local/bin/vyper); referenced in every `#pragma version` and `[vyper]` in foundry.toml.
2. `evm_version = "prague"` in foundry.toml (Arc targets Prague; gen-7). `optimize = "gas"`, **default codegen** (NOT experimental/venom — unverifiable, see recipe above).
3. 0.4.x syntax only: `@deploy def __init__`, `extcall`/`staticcall` prefixes, module imports, revert strings. Reserved param names (e.g. `value`) are a compile error — rename (`call_value`).
4. No inheritance, no snekmate unless genuinely required.
5. Tests + deploy scripts stay **Solidity** (`deployCode`/`vm.deployCode` against `.sol` interfaces); never write test contracts in Vyper.
6. Verification is **automated and mandatory**: `scripts/verify-contracts.ts` (Vyper, Blockscout v2 API) + `deploy.sh --verify` (Solidity). Not optional — every deploy ships verified.

@AGENTS.md
